server {
    listen 80;

    # docsだけ見たいよ
#     # /docs へのアクセスをFastAPIに転送
#     location /docs {
#         proxy_pass http://backend:8000/docs;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     # /openapi.json へのアクセスをFastAPIに転送 (docsに必要)
#     location /openapi.json {
#         proxy_pass http://backend:8000/openapi.json;
#     }
#     # ★★★ 追加ここまで ★★★
#
#     # /api/ へのアクセスは、すべてFastAPIバックエンドに転送
#     location /api {
#         proxy_pass http://backend:8000/api; # パスも追加するとより確実
#         # ... (ヘッダー設定は変更なし)
#     }

    # /api/ へのアクセスは、すべてFastAPIバックエンドに転送
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # それ以外のアクセスは、すべてReactアプリ（静的ファイル）に転送
    location / {
        # Reactアプリがビルドされたファイルが置かれる場所
        root /usr/share/nginx/html;
        # 常にindex.htmlを返すことでReact Routerが動作するようにする
        try_files $uri /index.html;
    }
}
